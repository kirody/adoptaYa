import { FormattedAgePipe } from './formatted-age.pipe';

describe('FormattedAgePipe', () => {
  it('create an instance', () => {
    const pipe = new FormattedAgePipe();
    expect(pipe).toBeTruthy();
  });
});
